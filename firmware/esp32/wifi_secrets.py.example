# Copy this file to wifi_secrets.py and fill in your own values.
# wifi_secrets.py is gitignored — never commit real credentials.

WIFI_SSID = "your-wifi-ssid"
WIFI_PASSWORD = "your-wifi-password"

# Password for WebREPL (remote REPL/filesystem access over WiFi).
# Grants full filesystem access to anyone who knows it - pick something
# stronger than the old "python3" default.
WEBREPL_PASSWORD = "your-webrepl-password"
